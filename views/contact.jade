//
   Created by Elizabeth on 8/6/2016.

extends layout

block content
    h2 Send me an Email

    form(action="/contact" method="post")
        | Your email address:
        br
        input(type="text" name="email" value="" id="email" required)
        br
        | Subject:
        br
        input(type="text" name="subject" size="38" id="subject" value="")
        br
        | Message body:
        br
        textarea(name="body" cols="40" rows="10" id="body" required)
        br
        button(type="submit" value="Send") Send
    if(sent == 1)
        p.sent Email sent! I'll get back to you as soon as possible.
    else if(sent==2)
        p.sent Error: Email not sent
